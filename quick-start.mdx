---
title: "快速开始"
description: "30 秒启动 Qingyu'Blog"
---

# 快速开始

## 静态模式（零配置）

```bash
git clone https://github.com/kejiland/qingyu-blog.git
cd qingyu-blog
# 双击 public/index.html 即可
```

或启动本地服务器：

```bash
python -m http.server 8080 -d public
# 或
npx serve public
```

打开 `http://localhost:8080/admin`，设置密码即可开始写作。

> 静态模式数据存储在浏览器 localStorage 中，适合本地写作和临时预览。

## 云端模式（推荐）

### 前提条件

- 注册 [Cloudflare](https://dash.cloudflare.com/sign-up) 账号
- 安装 Wrangler CLI：`npm install -g wrangler`

### 步骤一：创建资源

```bash
npx wrangler login
npx wrangler d1 create blog        # 记下 database_id
npx wrangler kv namespace create BLOG  # 记下 id
```

### 步骤二：配置 GitHub Secrets

详见 [GitHub Secrets 配置](/github-secrets)。

### 步骤三：部署

推送到 `main` 分支，GitHub Actions 自动：

1. 校验 Secrets
2. 执行 D1 迁移
3. 部署 Worker
4. 写入运行时 Secret

### 步骤四：首次设置密码

访问 `https://<worker名>.<子域>.workers.dev/admin` 设置密码。

## 模式对比

| 特性 | 静态模式 | 云端模式 |
|------|----------|----------|
| 运行环境 | 浏览器本地 | Cloudflare Workers |
| 数据存储 | localStorage | D1 数据库 |
| 多人访问 | ❌ | ✅ |
| 评论系统 | 本地存储 | 云端全局评论 |
| 零成本 | ✅ | ✅ |
