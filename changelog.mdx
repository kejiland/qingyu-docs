---
title: "更新日志"
description: "版本更新记录"
---

## v2.7.2（最新）

### 🛡️ 安全加固

- **登录限流重做为三层短冷却**：单 IP 5 次失败 → 15 分钟；同一子网（IPv4 /24、IPv6 前 4 段）15 次失败 → 60 秒冷却；全站 30 次失败 → **仅 10 秒冷却 + 告警日志**。**不再使用全局长锁定**——对只有一个管理员的系统，全局长锁定防不住攻击者（换个 IP 即可继续），却能被攻击者用来把站长挡在门外
- **新增应急通道**：登录请求携带正确的 `X-Setup-Key`（即 `BLOG_ADMIN_SETUP_KEY`）可**跳过全部限流**，但**不跳过密码校验**；后台登录页在触发限流时自动展开「被限流？用安装密钥登录」入口
- **锁定期与冷却期内直接返回 429 早退，不读也不写数据库**：分布式爆破最多每 10 秒消耗一次 D1 写入，避免被失败登录刷爆免费写额度；计数 1 小时老化，冷却结束后保留计数，防止「等冷却归零再重试」
- 修复了「全局限流三个常量从未声明」导致防 IP 轮换逻辑实际从未生效的问题

### ✨ 新功能

- **可选边缘限流绑定**：配置 GitHub Secret `BLOG_RATE_LIMIT_BINDING`（正整数，如 `1001`）后，部署工作流会自动改用 `wrangler@^4.36.0` 并注入 `[[ratelimits]]` 绑定（绑定名 `LOGIN_LIMITER`，10 次/分钟/位置）；**未配置时行为与旧版完全一致**，代码检测不到绑定即回退 D1 计数
- **可复现的截图工具** `scripts/screenshots/`：本地演示服务器（真实 `public/` 代码 + mock API 示例数据）+ 无头 Chrome 截图脚本，不需要管理员密码、不接触线上数据，可随时重跑；共生成 23 张截图

### 🔧 变更

- `BLOG_VERSION` → `2.7.2`，`public/index.html` 的静态资源 `?v=` → `2.7.2`，语言包缓存版本 `I18N_VER` → **9**
- `smoke-test.js` **77 → 78 例**（新增「登录限流」整套回归：锁定期间正确凭据、错误密钥不可绕过、计数不写库、计数老化、子网隔离、全局冷却不锁死、限流器异常降级）

### 📝 文档

- 主仓库文档**全面中英双语化**：`README`、`CLOUDFLARE_SETUP_GUIDE`、`DEPLOYMENT_SECRETS_GUIDE`、`SECURITY`、`ABOUT`、`CODE_OF_CONDUCT` 均提供 `X.md` + `X_EN.md` 成对文件；`CONTRIBUTING`、PR / Issue 模板、`FUNDING.yml`、`public/llms.txt` 改为单文件内中英对照；每个文件首行加入语言切换入口
- 新增 **Cloudflare 配置完全指南（新手版）**（中英双语，891 行）：账号注册 / D1 / KV / API Token 权限勾选 / R2 桶与 CORS / Workers AI / 自定义域名与 Zone ID / Secrets 全表 / 部署 / 首次初始化 / 12 项自检 / 故障排查 / 免费额度，并含第 9 节「加固后台登录入口」（Cloudflare Access / WAF 限流规则 / Workers 边缘限流绑定三种方案）
- 修正主仓库 README 中的过时描述：强调色 **4 种**（非 6 种）、冒烟测试 **78 例**、KV 真实键表、`SITE_URL` 未配置时 CORS fail-closed、迁移补到 0015

---

## v2.7.1

### 🔧 修复

- **R2 上传签名修复**：预签名 PUT 的签名头改为 `content-type;host`——`Content-Type` 参与签名，浏览器必须原样发送，**R2 桶 CORS 的 AllowedHeaders 必须包含 `Content-Type`**；签名路径按 RFC 3986 逐段编码，最终 URL 与签名路径完全一致；不签 `Content-Length`（浏览器差异会导致 403 且响应不带 CORS 头）
- **音乐上传写回音乐桶**：`R2_BUCKET` 与 `R2_PUBLIC_BASE` 都配置时音乐写入音乐桶，仅当音乐桶配置不完整时才回退 `R2_MEDIA_BUCKET`（此前会误写媒体桶）；上传失败会暴露真实失败阶段，便于定位预检失败
- 音频白名单补充 `oga`

### 🛡️ 安全加固

- 静态资源响应也统一注入安全响应头；未知 `/api/*` 一律返回 JSON 404，绝不回退 HTML（`functions/api/[[path]].js` 为 Pages 侧兜底）
- 新增**迁移 0015 热点索引**：`idx_comments_post_id` / `idx_comments_status_date` / `idx_music_sort` / `idx_media_created_id` / `idx_admin_sessions_exp`
- `robots.txt` 移除了不被支持的非标准指令

### ✨ 新功能

- **资源发现清单**：新增 `/.well-known/ard.json`（ARD v0.91）与 `/.well-known/ai-catalog.json`，并在首页通过 `<link rel="ard">` / `<link rel="ai-catalog">` 声明；`public/llms.txt` 提供面向 LLM / 代理的站点说明

### 🚀 体验优化

- **静态资源缓存分层**：带 `?v=` 版本号与字体 / 图标等不可变资源缓存一年；其余带扩展名资源 1 小时 + `stale-while-revalidate`；无扩展名 HTML 入口 `no-cache`
- 音乐播放器样式与脚本改为**按需加载**（不在首屏关键路径）
- 浅色主题文字对比度提升

### 📝 文档

- 新增 `DEPLOYMENT_SECRETS_GUIDE.md`（GitHub Secrets 与 R2 令牌详解，中英双语）

---

## v2.6.6

### 🚀 域名与 SEO

- **切换自定义域名**：全线改用 `https://www.2024921.xyz`（替换原 Cloudflare Workers 子域名入口），导航、管理后台入口、站点配置示例与文档同步更新
- **Sitemap / RSS 云端动态生成**：删除静态 `public/sitemap.xml` / `public/feed.xml`，新增根路径 `/sitemap.xml`、`/feed.xml` 动态端点；文章增删改查后自动生成最新站点地图与 RSS，无需手工维护
- **兼容旧 API**：`/api/sitemap.xml`、`/api/feed.xml` 继续可用，方便已有调用方平滑迁移

### 📝 文档

更新快速开始、Cloudflare Workers 部署、配置参考、API 总览、项目架构、本地静态部署、GitHub Secrets 与 docs.json 导航链接。

---

## v2.4

### ✨ 新功能

- **媒体图片 R2 直传**：后台媒体库图片上传从「base64 存 D1」迁移为**浏览器直传 R2**（预签名 PUT URL，图片不经过 Worker，规避请求体上限与带宽计费）；媒体使用**独立 R2 桶**（`R2_MEDIA_BUCKET`），与音乐桶分开
- **移除图片存 D1 的全部兼容**：`POST /api/media` 仅接受 http/https 链接（不再接受 `data:image` base64 内嵌）；历史 base64 记录由**迁移 `0014`** 一次性清除——媒体库现在只存 R2/外链记录，图片本体一律在 R2
- **删除文章级联清理**：删除文章时原子清理其评论（含回复）、点赞/浏览量计数与每日聚合（`stats_daily`）——同 ID 重建不会复活旧数据，后台趋势图不再计入已删文章流量

### 🔧 变更

- **BLOG_ADMIN_SETUP_KEY 改为可选**：配置后 `/api/admin/setup` 首次初始化需携带 `X-Setup-Key`（防抢注，推荐生产配置）；未配置时回退旧行为——首次部署自动生成随机默认密码（`xxxx-xxxx`），登录后强制修改。CI 不再把它列为必填 Secret

### 🛡️ 安全加固

- 安全响应头（CSP / X-Content-Type-Options 等）全量响应
- 回复标签存储型 XSS 修复、评论控制字符清洗
- 迁移改为**三层幂等策略**（`schema_migrations` 记账表 + `pragma_table_info` 列预检 + 报错兜底），重复部署不再因 `duplicate column` 中断
- 统计点赞/阅读改**原子自增**（并发不丢数）
- CI 部署前**前置运行三套测试**（smoke / gb / search），失败即中止不部署
- AI 功能文档补充隐私提示：摘要/写作助手/评论汇总会把文章或评论**明文发送给 Workers AI**，敏感内容请勿启用

### 📝 文档

本开发文档站同步更新：媒体（R2 直传）、管理员认证（密钥可选 / 随机密码流 / 改密接口）、站点设置接口、统计响应、迁移幂等、架构（media.js）、部署（CI 步骤 / R2 媒体桶）

---

## v2.3

### ✨ 新功能

- **全站音乐播放器**：右下角悬浮音符按钮，平时**缩进窗口外露出一点圆弧**、悬停 / 点击即滑出；点击弹出面板：曲目信息 · 可拖动进度条 · 上一首 / 播放暂停 / 下一首 · 音量 · 播放列表（点击切换、当前高亮 + 均衡动画）
- **音乐记忆**：自动连播；记忆上次曲目与进度、音量持久化，刷新后恢复但不自动出声
- **后台音乐管理**：音频上传（浏览器**直传 R2** 预签名 URL + 上传进度）；文件名自动识别「歌曲名-歌手」预填；行内试听 / 删除（**删除与 R2 对象同步**）；曲目多时列表**卡片内滚动、表头吸顶**

### 🚀 体验优化

- 播放器悬浮按钮与一键置顶解重合：置顶按钮上移至播放器上方（桌面 `bottom: 92px` / 移动端 `bottom: 80px`）
- 无音乐或接口失败时播放器完全隐藏；后台路由自动收起

### 📝 文档

- 新增[音乐](/music)文档页；API 总览 / 表结构 / 迁移 / 部署（R2）/ 架构 / README 同步更新

---

## v2.2

### ✨ 新功能

- **AI 写作助手**：编辑器一键生成**标题建议 / 润色 / 翻译**（目标语言可选），结果可应用或复制
- **AI 文章摘要**：文章页一键生成内容摘要，单篇 30 天缓存
- **AI 评论汇总**：评论管理页一键汇总近期评论要点（1 小时缓存）+ 单条评论垃圾检测
- **主题色切换**：强调色可切换（当前为 **4 种**：赭橙 `terra` / 黛蓝 `indigo` / 竹青 `bamboo` / 凝夜紫 `dusk`），桌面图标按钮 + 弹层 / 手机端原生下拉
- **后台品牌区**：左上角 Logo 渐变方块（站点头像或站点名首字 + 流动动画），左下角显示个人资料头像

### 🗑️ 移除

- **分类功能**：编辑器分类字段、文章列表分类筛选与「分类」列、分类管理页均已移除（`posts.category` 数据列保留兼容，不影响存量数据与 API 字段）
- AI 写作助手的**「标签」动作**移除（保留标题建议 / 润色 / 翻译）

### 🚀 体验优化

- 后台顶栏：右上角 **🌐 语言弹层**（同前台风格、SVG 国旗），移除新评论铃铛，待审核数移至侧栏「待审核评论」角标
- 侧栏可折叠，折叠后页脚仅剩头像、水平垂直居中
- 编辑器输入框自动增高，与预览同样完全展开（无内部滚动条）
- 手机端仪表盘内容卡片统一单列布局

### 📝 文档

- README / README_EN / 本开发文档站同步更新（AI、主题色、后台 UI 新增；分类相关描述移除）

---

## v2.1

### ✨ 新功能

- **留言板**：导航直达 `/guestbook`，「留言 / 优化方案」双分区，云端存储；通过合成 post ID 复用评论安全管道，零新增接口
- **重复发送拦截**：同一分区下相同昵称 + 相同内容的评论 / 留言返回 `409`，防误触双击与脚本刷同文
- **搜索升级**：结果展示关键字所在完整句子上下文，并高亮关键字；悬停无下划线

### 🚀 体验优化

- 管理后台评论**删除 / 审核无感刷新**：行级淡出移除 + 状态徽章就地更新，不再整表重载
- 管理后台文章**删除无感刷新**：行级淡出 + 静默校准列表；管理端请求时间戳穿透 `s-maxage` 边缘缓存
- **前台删除即时生效**：管理后台删除文章后，前台文章列表 / 详情 / 缓存同步更新，无需刷新网页
- 失败原因透传：评论 / 留言提交失败时，表单状态行直接显示后端具体原因（重复 / 频控 / 来源校验）

### 📝 文档

- 新增[留言板](/guestbook)文档页，重写[API 总览](/api-overview)

---

## v2.0

### ✨ 新功能

- 评论支持嵌套回复（`parent_id` 字段）
- 媒体资源库（图片上传，base64 存 D1）
- 站点设置持久化
- 每日统计聚合（30 天趋势图表）
- 管理员强制改密标记
- 清理遗留 nav 配置

### 📝 文档

- 新增开发文档站

---

## v1.0

### ✨ 核心功能

- 零框架、零构建、零依赖的博客系统
- 双通道架构：静态模式 + 云端模式
- Cloudflare Workers + D1 部署
- Markdown 编辑器
- 文章加密（PBKDF2 + AES-GCM）
- 评论系统（嵌套回复 + 审核）
- 阅读/点赞统计
- 站内搜索 + TOC
- RSS / Sitemap 自动生成
- 精选文章推荐

### 🎨 界面

- 深色/浅色主题切换
- 响应式设计（手机/平板/桌面）
- 四级衬线字体栈

### 🌐 国际化

- 内置 5 种语言
- 自动识别 + 手动切换

### 🔒 安全

- PBKDF2-SHA256 密码哈希
- 会话 Token 鉴权
- 登录限流
- CORS / XSS / SQL 注入防护
